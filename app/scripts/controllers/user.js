'use strict';

/**
 * @ngdoc function
 * @name zimmoApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the zimmoApp
 */
angular.module('zimmoApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
